/**
 * UI Module - Shared design system for the breathing app
 *
 * Usage:
 * ```tsx
 * import { COLORS, Z_LAYERS, OverlayBackdrop, CinematicText } from '../ui';
 * ```
 */

// Primitive components
export * from './primitives';
// Theme tokens
export * from './theme';
